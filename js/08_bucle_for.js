'use strict'

//BUCLE FOR
// Bucle es una estructura de control que se repite variac veces

var numeros = 100
//Aca el i es el que se va a incrementar en cada uno de los ciclos, el cual en este caso lo iniciamos en 0
//i++ aumenta el valor en 1 en cada vuelta
//i < 100 es la condicion que se debe cumplir para seguir en el bucle
for (var i = 0; i <= 100; i++) {
  console.log('vamos en el numero: ' + i)
  // Aca se colocas  "debugger" puede ir viendo lo que va pasando en cada paso
}
